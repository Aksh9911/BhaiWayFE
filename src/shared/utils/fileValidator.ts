/** @deprecated Import from `@/utils/fileValidation` instead. */
export {
  isImageMimeType,
  isPdfMimeType,
  resolveMimeType,
  getFileSizeBytes,
  validateFileForKind,
  assertAllowedImage,
  assertAllowedDocument,
} from '@/utils/fileValidation';
