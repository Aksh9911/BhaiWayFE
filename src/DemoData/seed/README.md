# Seed (legacy)

Prefer **`src/DemoData/files`** for shared demo constants, CSV header templates, and Apps Script.

This folder kept older sample CSVs for reference only. The app does not invent seed rows — data comes from user entry + Google Sheet sync.
